import { ImageDisplayClass } from './image-display-class.model';

describe('ImageDisplayClass', () => {
  it('should create an instance', () => {
    // expect(new ImageDisplayClass()).toBeTruthy();
  });
});
